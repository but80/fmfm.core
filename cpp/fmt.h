#pragma once
#include "go2cpp.h"

namespace fmt {

    void Print(...);
    void Println(...);
    void Printf(string fmt, ...);
    string Sprint(...);
    string Sprintln(...);
    string Sprintf(string fmt, ...);

}
